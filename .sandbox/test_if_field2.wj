struct ContactForm {
    name_error: string
}

impl ContactForm {
    fn test() {
        foo([
            bar(if self.name_error.len() > 0 { "a" } else { "b" })
        ])
    }
}

fn foo(attrs: Vec<string>) {}
fn bar(s: string) -> string { s }

