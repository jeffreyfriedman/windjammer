struct App {
    name_error: string
}

impl App {
    fn render() -> View {
        view {
            input([
                class(if self.name_error.len() > 0 { "error" } else { "" })
            ])
        }
    }
}
