// Test function pointer types

fn add(x: int, y: int) -> int {
    x + y
}

fn apply(f: fn(int, int) -> int, a: int, b: int) -> int {
    f(a, b)
}

fn main() {
    let op: fn(int, int) -> int = add
    let result = apply(op, 5, 3)
    println!("Result: {}", result)
}

