// std/time - Date and time utilities
// Wraps chrono for ergonomic time handling

// Re-export chrono for direct use
// Users can call chrono::Utc::now(), chrono::Local::now(), etc.
// Example: let now = chrono::Local::now();
