// std/http - HTTP client for making web requests
// Wraps reqwest for async HTTP operations

// Re-export reqwest for direct use
// Users can call reqwest::get, reqwest::Client, etc.
// Example: reqwest::get("https://example.com").await?
