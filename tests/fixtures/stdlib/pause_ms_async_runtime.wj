use std::async_runtime::sleep_ms_blocking

pub fn pause_ms(ms: int) {
    if ms <= 0 {
        return
    }
    // Runtime FFI expects u64; use a literal so this import repro cargo-checks cleanly.
    sleep_ms_blocking(1)
}
