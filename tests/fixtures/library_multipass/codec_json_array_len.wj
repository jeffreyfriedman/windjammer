use std::json

pub fn array_len(text: string) -> int {
    match json.parse(text) {
        Ok(root) => {
            if !json.is_array(root) {
                return 0
            }
            json.len(root)
        },
        Err(_) => 0,
    }
}
