// Test: Automatic mut inference for local variables

pub fn counter() -> int {
    let mut count = 0
    count = count + 1
    count = count + 1
    count
}

pub fn accumulate(values: Vec<int>) -> int {
    let mut sum = 0
    for value in values {
        sum = sum + value
    }
    sum
}





























