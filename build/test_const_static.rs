const MAX_SIZE: i64 = 1024;
static BUFFER_SIZE: i64 = /* expression */;
const TIMEOUT_MS: i64 = /* expression */;
const PI_TIMES_TWO: f64 = /* expression */;

fn main() {
    println("MAX_SIZE = {}", MAX_SIZE);
    println("BUFFER_SIZE = {}", BUFFER_SIZE);
    println("TIMEOUT_MS = {}", TIMEOUT_MS);
    println("PI_TIMES_TWO = {}", PI_TIMES_TWO)
}

